// PLACEHOLDER — this sandbox can reach PyPI but not generic CDN hosts (unpkg/cdnjs timed out).
// Before running the app for real: download htmx 2.x from https://htmx.org and replace this
// file, or install via `npm install htmx.org` and point STATICFILES_DIRS at node_modules.
// Tracked as a manual step, not silently skipped.
